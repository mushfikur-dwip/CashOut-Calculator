# CashOut Calculator

This project calculates mobile wallet cash-out fees. The UI is served with `index.html` and the main logic lives in `app.js`.

## Development

Install dependencies and run tests using npm:

```bash
npm install
npm test
```

The tests validate fee calculations for all payment methods and modes.
